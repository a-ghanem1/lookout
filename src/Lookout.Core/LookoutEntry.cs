namespace Lookout.Core;

/// <summary>
/// A single captured diagnostic entry. All properties are added in M2.2.
/// This stub exists so callback delegate types in <see cref="LookoutOptions"/> compile.
/// </summary>
public sealed record LookoutEntry;
