import { ArrowUpRight } from 'lucide-react';

export function HttpClientsIcon({ className }: { className?: string }) {
  return <ArrowUpRight size={16} strokeWidth={1.75} className={className} aria-hidden />;
}
