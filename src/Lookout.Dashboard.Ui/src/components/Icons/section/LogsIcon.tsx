import { AlignJustify } from 'lucide-react';

export function LogsIcon({ className }: { className?: string }) {
  return <AlignJustify size={16} strokeWidth={1.75} className={className} aria-hidden />;
}
