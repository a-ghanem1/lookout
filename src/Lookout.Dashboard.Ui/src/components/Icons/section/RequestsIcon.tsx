import { RotateCw } from 'lucide-react';

export function RequestsIcon({ className }: { className?: string }) {
  return <RotateCw size={16} strokeWidth={1.75} className={className} aria-hidden />;
}
